function atualizaValores(){
    var select = document.querySelector('#origem');
    var optionValue = select.options[select.selectedIntex].value;

    var value = optionValue.value;
    var text = optionValue.text;

    console.log(optionValue, text);
}

atualizaValores()

if (mato>pessoa){
    function capinar();
}

